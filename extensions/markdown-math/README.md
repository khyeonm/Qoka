# Markdown Math

**Notice:** This extension is bundled with Aria. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to Aria's built-in markdown preview and markdown cells in notebooks.
